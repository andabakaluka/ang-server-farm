import {Server} from './server'

export class Farm {
    id: number;
    name: string;
    uls: string;

    //servers: Server[];
  }

export class Farm2 {
    id: number;
    name: string;
    uls: string;

    servers: Server[];
}